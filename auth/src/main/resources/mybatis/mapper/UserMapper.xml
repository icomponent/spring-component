<?xml version="1.0" encoding="UTF-8"?>

<mapper namespace="club.icomponent.auth.mapper.UserMapper">
    <select id="selectUserByUsername" resultType="club.icomponent.common.auth.User">
        SELECT id, name, username, password
        FROM user
        WHERE username = #{username};
    </select>
</mapper>